#!/bin/bash

DOCKER_REPO="${DOCKER_REPO:-andzuc/rrsync}"
DOCKER_VERSION="${DOCKER_VERSION:-latest}"

tar -C context -cf - . \
    |docker build \
	    --progress plain \
	    -t "${DOCKER_REPO}:${DOCKER_VERSION}" -
